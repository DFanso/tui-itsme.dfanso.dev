# itsme.dfanso.dev — TUI edition

A native terminal version of [itsme.dfanso.dev](https://itsme.dfanso.dev), written in C++17 with FTXUI.

## Build

```
cmake --preset default
cmake --build --preset default
ctest --preset default
```
